<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🔎 Izinkan lihat app dengan launcher (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- ⚠️ Opsional: QUERY_ALL_PACKAGES → jangan dipakai di Play Store -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- 🌐 Internet buat kirim webhook -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 🔁 Jalan otomatis saat boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:label="WSL Listener!"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotifyHook"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 📡 Notification listener service -->
        <service
            android:name=".NotificationListener"
            android:label="WSL Listener!"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- 🔄 Foreground service permanen -->
        <service
            android:name=".PersistentService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 🔁 Boot receiver → restart listener kalau toggle ON -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 🎯 Main UI -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
